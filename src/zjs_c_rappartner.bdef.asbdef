projection;
strict;
use draft;

define behavior for ZJS_C_RAPPARTNER alias Partner
{
  use create;
  use update;
  use delete;

  use action fillEmptyStreets;
  use action clearAllEmptyStreets;
  use action copyLine;
  use action withPopup;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;
  use action Resume;
}